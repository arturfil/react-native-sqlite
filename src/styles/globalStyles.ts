import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  viewContainer: {
    margin: 20,
    height: '100%',
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  }
})